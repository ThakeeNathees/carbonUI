Import('env')

SOURCES = []

## compile libs
SConscript('glad/SConstruct')
SConscript('glfw/SConstruct')
SConscript('ImGui/SConstruct')

## TODO: compile from source (live above)
env.Append(LIBS=['carbon'])
env.Append(LIBPATH=[Dir('./carbon/bin/')])
env.Append(CPPPATH=[Dir('./carbon/include/')])

env.LIBS["thirdparty"] += SOURCES